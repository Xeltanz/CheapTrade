spring:
  datasource:
    platform: h2
    url: jdbc:h2:mem:cheapdb

  h2:
    console:
      enabled: true
      path: /h2

  mail:
    host: smtp.gmail.com
    port: 587
    username: cheapt861@gmail.com
    password: cheaptrade19
    protocol: smtp
    properties.mail.smtp:
      auth: true
      connectiontimeout: 10000
      timeout: 10000
      writetimeout: 10000
      starttls.enable: true
      ssl.trust: smtp.gmail.com
#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.type.descriptor.sql: trace
